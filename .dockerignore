# Host node_modules must never enter the image: native binaries are built for the
# host arch (macOS/arm64) and break the Linux build. Deps are installed inside.
node_modules
.next

.git
.gitignore

.env*
!.env.example

*.log
logs

# Dev-only surface
e2e
.playwright-mcp
.claude
docs
supabase/.temp
